.venv/
__pycache__/
*.pyc
.env
*.egg-info/
dist/
build/

# eval/**/*.py run output -- real run results (may include real tenant/
# vendor data from your cybersierra backend, and local Netra org/dataset
# state), not the eval framework code itself
eval/results/

# harness/observability.py's rotating log file -- runtime output, not
# something to commit (also may contain real CLI output/tenant data,
# though access_token is scrubbed -- see harness/sandbox.py's scrub())
logs/
